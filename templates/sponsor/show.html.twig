{% extends 'base.html.twig' %}

{% block title %}Sponsor{% endblock %}

{% block body %}
   

    <table class="table" style="border: 2px solid black; background-color: white;">
        <tbody>
            <tr>
                <th style="color: black;"><strong>Nom</strong></th>
                <td style="color: black;"><strong>{{ sponsor.nom }}</strong></td>
            </tr>
            <tr>
                <td colspan="2" style="border-bottom: 2px solid black;"></td> <!-- Ligne de séparation -->
            </tr>
            <tr>
                <th style="color: black;"><strong>Adresse</strong></th>
                <td style="color: black;"><strong>{{ sponsor.adresse }}</strong></td>
            </tr>
            <tr>
                <td colspan="2" style="border-bottom: 2px solid black;"></td> <!-- Ligne de séparation -->
            </tr>
            <tr>
                <th style="color: black;"><strong>Mail</strong></th>
                <td style="color: black;"><strong>{{ sponsor.mail }}</strong></td>
            </tr>
            <tr>
                <td colspan="2" style="border-bottom: 2px solid black;"></td> <!-- Ligne de séparation -->
            </tr>
            <tr>
                <th style="color: black;"><strong>Telephone</strong></th>
                <td style="color: black;"><strong>{{ sponsor.telephone }}</strong></td>
            </tr>
            <tr>
                <td colspan="2" style="border-bottom: 2px solid black;"></td> <!-- Ligne de séparation -->
            </tr>
            <tr>
                <th style="color: black;"><strong>DateCreation</strong></th>
                <td style="color: black;"><strong>{{ sponsor.dateCreation ? sponsor.dateCreation|date('Y-m-d H:i:s') : '' }}</strong></td>
            </tr>
        </tbody>
    </table>

    <div style="text-align: center;"> <!-- Centrer les liens -->
        <a href="{{ path('app_sponsor_index') }}" style="color: white;">back to list</a>
        
        <a href="{{ path('app_sponsor_edit', {'id': sponsor.id}) }}" style="color: white;">edit</a>
    </div>

    {{ include('sponsor/_delete_form.html.twig') }}
{% endblock %}
