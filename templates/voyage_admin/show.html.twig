{% extends 'base1.html.twig' %}

{% block title %}Voyage{% endblock %}

{% block body %}
  <div style="text-align: center; margin-top: 50px;"> <!-- Centre le contenu et ajoute un espacement au-dessus -->

    <h1>Liste des voyages</h1>
<br>
<br>
    <table class="table">
        <tbody>
            <tr>
                <th>Réference du voyage</th>
                <td>{{ voyage.id }}</td>
            </tr>
            <tr>
                <th>VilleDepart</th>
                <td>{{ voyage.villeDepart }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ voyage.destination }}</td>
            </tr>
            <tr>
                <th>HeureDepart</th>
                <td>{{ voyage.heureDepart ? voyage.heureDepart|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HeureArrivee</th>
                <td>{{ voyage.heureArrivee ? voyage.heureArrivee|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TypeVoyage</th>
                <td>{{ voyage.typeVoyage }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ voyage.type }}</td>
            </tr>
            <tr>
                <th>Activites</th>
                <td>{{ voyage.activites ? voyage.activites|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voyage_admin_index') }}">back to list</a>


    {{ include('voyage_admin/_delete_form.html.twig') }}
{% endblock %}
